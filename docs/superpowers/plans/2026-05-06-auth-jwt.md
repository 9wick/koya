# @zeltjs/auth-jwt Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** JWT Bearer認証パッケージを作成し、`@zeltjs/core` の認証プリミティブ（`setUser`, `@Authorized`）と統合する。

**Architecture:** `jose` ライブラリを使用してJWT操作を行い、`JwtConfig` でカスタマイズ可能な設定、`JwtService` でsign/verify/decode、`JwtMiddleware` でAuthorization ヘッダーからトークン抽出・検証・ユーザー設定を行う。

**Tech Stack:** TypeScript, jose@6.0.11, @zeltjs/core (peerDependency), vitest, tsdown

---

## File Structure

```
packages/auth-jwt/
├── src/
│   ├── index.ts              # エクスポート
│   ├── jwt.types.ts          # JwtPayload 型定義
│   ├── jwt.config.ts         # JwtConfig クラス
│   ├── jwt.service.ts        # JwtService クラス
│   ├── jwt.service.test.ts   # JwtService テスト
│   ├── jwt.middleware.ts     # JwtMiddleware クラス
│   └── jwt.middleware.test.ts # JwtMiddleware テスト
├── package.json
├── tsconfig.json
└── tsdown.config.ts
```

---

## Task 1: Package Setup

**Files:**
- Create: `packages/auth-jwt/package.json`
- Create: `packages/auth-jwt/tsconfig.json`
- Create: `packages/auth-jwt/tsdown.config.ts`
- Modify: `tsconfig.json` (root)

- [ ] **Step 1: Create package.json**

```json
{
  "name": "@zeltjs/auth-jwt",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zeltjs/zelt.git",
    "directory": "packages/auth-jwt"
  },
  "publishConfig": {
    "access": "public"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsdown",
    "test": "vitest run",
    "typecheck": "tsc -b"
  },
  "peerDependencies": {
    "@zeltjs/core": "workspace:*"
  },
  "dependencies": {
    "jose": "6.0.11"
  },
  "devDependencies": {
    "@zeltjs/core": "workspace:*",
    "@types/node": "22.19.17",
    "hono": "4.12.16"
  }
}
```

- [ ] **Step 2: Create tsconfig.json**

```json
{
  "extends": "@9wick/eslint-plugin-strict-type-rules/tsconfig/strictest.json",
  "compilerOptions": {
    "composite": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "tsBuildInfoFile": "./dist/.tsbuildinfo",
    "experimentalDecorators": true,
    "erasableSyntaxOnly": false,
    "types": ["node"]
  },
  "include": ["src/**/*"]
}
```

- [ ] **Step 3: Create tsdown.config.ts**

```typescript
import { defineConfig } from 'tsdown';

export default defineConfig({
  entry: ['src/index.ts'],
  format: ['esm'],
  dts: true,
  clean: true,
  fixedExtension: false,
  deps: {
    neverBundle: ['hono', /^hono\//, /^@hono\//, '@zeltjs/core', /^@zeltjs\/core\//],
  },
});
```

- [ ] **Step 4: Update root tsconfig.json**

Add reference to `packages/auth-jwt`:

```json
{
  "compilerOptions": {},
  "references": [
    { "path": "packages/core" },
    { "path": "packages/contract" },
    { "path": "packages/testing" },
    { "path": "packages/adapter-node" },
    { "path": "packages/cli" },
    { "path": "packages/auth-jwt" }
  ],
  "files": []
}
```

- [ ] **Step 5: Install dependencies**

Run: `pnpm install`
Expected: Dependencies installed, lockfile updated

- [ ] **Step 6: Commit**

```bash
git add packages/auth-jwt/package.json packages/auth-jwt/tsconfig.json packages/auth-jwt/tsdown.config.ts tsconfig.json pnpm-lock.yaml
git commit -m "feat(auth-jwt): initialize package structure"
```

---

## Task 2: JwtPayload Type

**Files:**
- Create: `packages/auth-jwt/src/jwt.types.ts`

- [ ] **Step 1: Create jwt.types.ts**

```typescript
export interface JwtPayload {
  sub?: string;
  iat?: number;
  exp?: number;
  [key: string]: unknown;
}
```

- [ ] **Step 2: Verify typecheck passes**

Run: `pnpm --filter @zeltjs/auth-jwt typecheck`
Expected: No errors (may warn about no files yet, that's ok)

- [ ] **Step 3: Commit**

```bash
git add packages/auth-jwt/src/jwt.types.ts
git commit -m "feat(auth-jwt): add JwtPayload type"
```

---

## Task 3: JwtConfig

**Files:**
- Create: `packages/auth-jwt/src/jwt.config.ts`

- [ ] **Step 1: Create jwt.config.ts**

```typescript
import { Config } from '@zeltjs/core';
import type { RequestContextSchema } from '@zeltjs/core';

import type { JwtPayload } from './jwt.types';

export interface ResolveUserResult {
  user: RequestContextSchema['user'];
  roles: RequestContextSchema['authRoles'];
}

@Config
export class JwtConfig {
  static readonly Token = JwtConfig;

  get secret(): string {
    const secret = process.env['JWT_SECRET'];
    if (!secret) {
      throw new Error('JWT_SECRET environment variable is required');
    }
    return secret;
  }

  get expiresIn(): string {
    return '1h';
  }

  get resolveUser(): (payload: JwtPayload) => Promise<ResolveUserResult> {
    return async (payload) => ({
      user: payload.sub,
      roles: [],
    });
  }
}
```

- [ ] **Step 2: Verify typecheck passes**

Run: `pnpm --filter @zeltjs/auth-jwt typecheck`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add packages/auth-jwt/src/jwt.config.ts
git commit -m "feat(auth-jwt): add JwtConfig class"
```

---

## Task 4: JwtService - Failing Tests

**Files:**
- Create: `packages/auth-jwt/src/jwt.service.test.ts`

- [ ] **Step 1: Write failing tests for JwtService**

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { Container } from '@needle-di/core';

import { JwtService } from './jwt.service';
import { JwtConfig } from './jwt.config';

class TestJwtConfig extends JwtConfig {
  override get secret(): string {
    return 'test-secret-key-for-testing-only';
  }

  override get expiresIn(): string {
    return '1h';
  }
}

describe('JwtService', () => {
  let jwtService: JwtService;

  beforeEach(() => {
    const container = new Container();
    container.bind({ provide: JwtConfig.Token, useClass: TestJwtConfig });
    jwtService = container.get(JwtService);
  });

  describe('sign', () => {
    it('should generate a valid JWT token', async () => {
      const token = await jwtService.sign({ sub: 'user-123' });

      expect(token).toBeDefined();
      expect(typeof token).toBe('string');
      expect(token.split('.')).toHaveLength(3);
    });
  });

  describe('verify', () => {
    it('should verify and return payload for valid token', async () => {
      const token = await jwtService.sign({ sub: 'user-123', customClaim: 'value' });
      const payload = await jwtService.verify(token);

      expect(payload.sub).toBe('user-123');
      expect(payload.customClaim).toBe('value');
      expect(payload.iat).toBeDefined();
      expect(payload.exp).toBeDefined();
    });

    it('should throw error for invalid token', async () => {
      await expect(jwtService.verify('invalid-token')).rejects.toThrow();
    });

    it('should throw error for tampered token', async () => {
      const token = await jwtService.sign({ sub: 'user-123' });
      const tamperedToken = token.slice(0, -5) + 'xxxxx';

      await expect(jwtService.verify(tamperedToken)).rejects.toThrow();
    });
  });

  describe('decode', () => {
    it('should decode token without verification', async () => {
      const token = await jwtService.sign({ sub: 'user-123' });
      const payload = jwtService.decode(token);

      expect(payload).not.toBeNull();
      expect(payload?.sub).toBe('user-123');
    });

    it('should return null for invalid token', () => {
      const payload = jwtService.decode('not-a-valid-jwt');

      expect(payload).toBeNull();
    });
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pnpm --filter @zeltjs/auth-jwt test`
Expected: FAIL with "Cannot find module './jwt.service'"

- [ ] **Step 3: Commit failing tests**

```bash
git add packages/auth-jwt/src/jwt.service.test.ts
git commit -m "test(auth-jwt): add failing tests for JwtService"
```

---

## Task 5: JwtService - Implementation

**Files:**
- Create: `packages/auth-jwt/src/jwt.service.ts`

- [ ] **Step 1: Implement JwtService**

```typescript
import { SignJWT, jwtVerify, decodeJwt } from 'jose';

import { Injectable, injectConfig } from '@zeltjs/core';

import { JwtConfig } from './jwt.config';
import type { JwtPayload } from './jwt.types';

@Injectable()
export class JwtService {
  constructor(private config = injectConfig(JwtConfig)) {}

  async sign(payload: Record<string, unknown>): Promise<string> {
    const secret = new TextEncoder().encode(this.config.secret);
    const expiresIn = this.parseExpiresIn(this.config.expiresIn);

    const jwt = await new SignJWT(payload)
      .setProtectedHeader({ alg: 'HS256' })
      .setIssuedAt()
      .setExpirationTime(expiresIn)
      .sign(secret);

    return jwt;
  }

  async verify(token: string): Promise<JwtPayload> {
    const secret = new TextEncoder().encode(this.config.secret);
    const { payload } = await jwtVerify(token, secret);
    return payload as JwtPayload;
  }

  decode(token: string): JwtPayload | null {
    try {
      return decodeJwt(token) as JwtPayload;
    } catch {
      return null;
    }
  }

  private parseExpiresIn(expiresIn: string): string | number {
    const match = /^(\d+)([smhd])$/.exec(expiresIn);
    if (match) {
      const value = parseInt(match[1], 10);
      const unit = match[2];
      const unitMap: Record<string, string> = {
        s: 'seconds',
        m: 'minutes',
        h: 'hours',
        d: 'days',
      };
      return `${value} ${unitMap[unit]}`;
    }
    return expiresIn;
  }
}
```

- [ ] **Step 2: Run tests to verify they pass**

Run: `pnpm --filter @zeltjs/auth-jwt test`
Expected: All tests PASS

- [ ] **Step 3: Commit**

```bash
git add packages/auth-jwt/src/jwt.service.ts
git commit -m "feat(auth-jwt): implement JwtService"
```

---

## Task 6: JwtMiddleware - Failing Tests

**Files:**
- Create: `packages/auth-jwt/src/jwt.middleware.test.ts`

- [ ] **Step 1: Write failing tests for JwtMiddleware**

```typescript
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { Hono } from 'hono';
import { Container } from '@needle-di/core';

import { JwtMiddleware } from './jwt.middleware';
import { JwtService } from './jwt.service';
import { JwtConfig } from './jwt.config';

class TestJwtConfig extends JwtConfig {
  override get secret(): string {
    return 'test-secret-key-for-testing-only';
  }

  override get resolveUser() {
    return async (payload: { sub?: string }) => ({
      user: { id: payload.sub, name: 'Test User' },
      roles: ['user', 'admin'],
    });
  }
}

describe('JwtMiddleware', () => {
  let app: Hono;
  let jwtService: JwtService;
  let middleware: JwtMiddleware;

  beforeEach(() => {
    const container = new Container();
    container.bind({ provide: JwtConfig.Token, useClass: TestJwtConfig });
    jwtService = container.get(JwtService);
    middleware = container.get(JwtMiddleware);

    app = new Hono();
    app.use('/*', (c, next) => middleware.use(c, next));
    app.get('/protected', (c) => c.json({ message: 'success' }));
  });

  it('should return 401 when no Authorization header', async () => {
    const res = await app.request('/protected');

    expect(res.status).toBe(401);
  });

  it('should return 401 when Authorization header is not Bearer', async () => {
    const res = await app.request('/protected', {
      headers: { Authorization: 'Basic abc123' },
    });

    expect(res.status).toBe(401);
  });

  it('should return 401 when token is invalid', async () => {
    const res = await app.request('/protected', {
      headers: { Authorization: 'Bearer invalid-token' },
    });

    expect(res.status).toBe(401);
  });

  it('should allow request with valid token', async () => {
    const token = await jwtService.sign({ sub: 'user-123' });
    const res = await app.request('/protected', {
      headers: { Authorization: `Bearer ${token}` },
    });

    expect(res.status).toBe(200);
    const body = await res.json();
    expect(body.message).toBe('success');
  });

  it('should return 401 for expired token', async () => {
    const container = new Container();
    class ExpiredConfig extends JwtConfig {
      override get secret(): string {
        return 'test-secret-key-for-testing-only';
      }
      override get expiresIn(): string {
        return '0s';
      }
    }
    container.bind({ provide: JwtConfig.Token, useClass: ExpiredConfig });
    const expiredJwtService = container.get(JwtService);
    const token = await expiredJwtService.sign({ sub: 'user-123' });

    await new Promise((resolve) => setTimeout(resolve, 100));

    const res = await app.request('/protected', {
      headers: { Authorization: `Bearer ${token}` },
    });

    expect(res.status).toBe(401);
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `pnpm --filter @zeltjs/auth-jwt test`
Expected: FAIL with "Cannot find module './jwt.middleware'"

- [ ] **Step 3: Commit failing tests**

```bash
git add packages/auth-jwt/src/jwt.middleware.test.ts
git commit -m "test(auth-jwt): add failing tests for JwtMiddleware"
```

---

## Task 7: JwtMiddleware - Implementation

**Files:**
- Create: `packages/auth-jwt/src/jwt.middleware.ts`

- [ ] **Step 1: Implement JwtMiddleware**

```typescript
import { HTTPException } from 'hono/http-exception';

import { Middleware, inject, injectConfig, setUser } from '@zeltjs/core';
import type { RequestContext, Next } from '@zeltjs/core';

import { JwtConfig } from './jwt.config';
import { JwtService } from './jwt.service';

@Middleware
export class JwtMiddleware {
  constructor(
    private jwtService = inject(JwtService),
    private config = injectConfig(JwtConfig),
  ) {}

  async use(c: RequestContext, next: Next): Promise<Response | undefined> {
    const token = this.extractBearerToken(c);
    if (!token) {
      throw new HTTPException(401, { message: 'Missing or invalid Authorization header' });
    }

    try {
      const payload = await this.jwtService.verify(token);
      const { user, roles } = await this.config.resolveUser(payload);
      setUser(user, roles);
    } catch {
      throw new HTTPException(401, { message: 'Invalid or expired token' });
    }

    await next();
    return undefined;
  }

  private extractBearerToken(c: RequestContext): string | null {
    const authHeader = c.req.header('Authorization');
    if (!authHeader) {
      return null;
    }

    const parts = authHeader.split(' ');
    if (parts.length !== 2 || parts[0] !== 'Bearer') {
      return null;
    }

    return parts[1];
  }
}
```

- [ ] **Step 2: Run tests to verify they pass**

Run: `pnpm --filter @zeltjs/auth-jwt test`
Expected: All tests PASS

- [ ] **Step 3: Commit**

```bash
git add packages/auth-jwt/src/jwt.middleware.ts
git commit -m "feat(auth-jwt): implement JwtMiddleware"
```

---

## Task 8: Index Exports

**Files:**
- Create: `packages/auth-jwt/src/index.ts`

- [ ] **Step 1: Create index.ts with all exports**

```typescript
export { JwtConfig } from './jwt.config';
export type { ResolveUserResult } from './jwt.config';
export { JwtService } from './jwt.service';
export { JwtMiddleware } from './jwt.middleware';
export type { JwtPayload } from './jwt.types';
```

- [ ] **Step 2: Verify build passes**

Run: `pnpm --filter @zeltjs/auth-jwt build`
Expected: Build succeeds, dist/ folder created with index.js and index.d.ts

- [ ] **Step 3: Verify typecheck passes**

Run: `pnpm --filter @zeltjs/auth-jwt typecheck`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add packages/auth-jwt/src/index.ts
git commit -m "feat(auth-jwt): add index exports"
```

---

## Task 9: Final Verification

**Files:**
- None (verification only)

- [ ] **Step 1: Run all tests**

Run: `pnpm --filter @zeltjs/auth-jwt test`
Expected: All tests PASS

- [ ] **Step 2: Run typecheck**

Run: `pnpm --filter @zeltjs/auth-jwt typecheck`
Expected: PASS

- [ ] **Step 3: Run build**

Run: `pnpm --filter @zeltjs/auth-jwt build`
Expected: Build succeeds

- [ ] **Step 4: Verify dist output**

Run: `ls -la packages/auth-jwt/dist/`
Expected: index.js, index.d.ts, and source maps present

- [ ] **Step 5: Final commit (if any uncommitted changes)**

```bash
git status
# If clean, skip. Otherwise:
git add -A && git commit -m "chore(auth-jwt): final cleanup"
```
